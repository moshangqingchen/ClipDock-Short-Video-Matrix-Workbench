# 快手主页资源与登录识别修复

2026-09-12，原安装目录 `D:\SJdownloads\创作平台`。

## 原因与修改

- 快手 `/new-reco` 实际加载的 `wskwai.com`、`wsbkwai.com` 样式、脚本、图片被工作台判为 `UNKNOWN_TARGET`；另有 `ndcimgs.com`、`oskwai.com` 视频资源被拦截。将这些资源域名补入快手专属 CONNECT 候选范围，仍要求当前账号的 DIRECT 连接验证，不扩大登录或顶层导航来源。
- 新版主页将当前账号放在 `.workbench > main > .wb-left > .sidebar`。原顶部头像选择器无法识别，通用 `main` 排除又把导航一起排除了。新增独立侧栏分支，将当前账号名称和头像与固定 self-profile 响应槽匹配；缺失、隐藏、未完成加载、账号切换不一致或推荐作者内容均不确认在线。
- 识别成功继续通过既有头像缓存入口处理当前账号头像，不修改 Cookie，不跳转管理页。

## 验证

- 登录检测、主页识别、资源范围和国内网络接入共 201 个测试通过；类型检查及生产构建通过。
- 原安装程序实测：快手状态 `online`、检查 `confirmed`；主页 12 个样式表、7 个脚本、41/41 张图片加载成功，页面有 2 个视频元素。
- 资源重载记录不再出现样式或脚本被工作台拦截；仍有平台 SVG 返回 404，以及页面切换视频产生的 Media `ERR_ABORTED`。
- 14 个国内账号及 2 个国外账号的 ID、平台和名称与更新前完全一致。
- 当前账号头像地址已能提取，主页匿名头像请求返回 JPEG 200，但工作台侧栏缓存仍未生成；未将头像地址可读误报为头像显示成功。
- 安装文件哈希及时间见 `output/kuaishou-home-installed-update.json`；资源记录见 `output/kuaishou-resource-diagnostic.json`。

## 更新前发现的独立数据库异常

更新前 `workbench.db` 的 `integrity_check` 已报告页引用及索引异常，普通账号查询仍可读取。没有对数据库进行修复、回滚或替换。完整原始数据库文件及程序已按字节复制并校验哈希，备份位置见 `output/kuaishou-home-backup.log`。此备份的 `DatabaseIntegrity:false` 明确表示原始数据库本身未通过完整性检查；`Verified:true` 仅表示备份文件与原文件一致。
