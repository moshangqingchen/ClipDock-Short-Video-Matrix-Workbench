# 采集数据库恢复与多平台修复

## 数据恢复

- 先正常退出程序，按字节备份原程序、数据库和日志文件，未改动账号浏览器分区或登录 Cookie。
- 在副本重建全部表和索引。指标表通过主键读取到 2,581 条有效记录，与最近健康备份中的 2,493 条逐条哈希一致，另外保留新增 88 条。
- 采集任务表错误关联到一个日志数据页。确认其中 32 条实际属于日志后，按正确表恢复；还找回日志表中原本无法到达的最后 3 条记录。所有备份中已有采集任务保留。
- 恢复后的 15 个国内账号、2 个国外账号、46 个作品、设置及发布记录与源文件的有效记录一致。15 个账号的采集指标 INSERT 全部通过，并回滚测试写入。
- `integrity_check` 通过，`foreign_key_check` 为 0，逐表记录哈希验证通过。恢复和激活报告分别见 `output/collection-database-recovery.json`、`output/collection-database-activation.json`。
- 完成第一轮真实采集后正常退出，再做健康备份，数据库校验仍通过。备份记录为 `output/collection-healthy-backup.log`。

## 采集修复

- 视频号及快手：当管理页缺少新鲜身份资料时，重新加载同一管理页并等待网页正常的身份响应，避免复用已经过期的 30 秒观察结果。保留当前公开主页的规则不变，不把公开页自动切成管理页。
- 身份竞态：用当前文档的账号 subject 校验结果归属；同账号的新响应或不确定响应不再误报切换账号。真正换账号、退出登录、导航、断开或网络撤销仍使旧上下文失效，不能据此刷新登录有效期。
- 快手：复用网页确认的资料，停止以空请求体重复查询身份；接入实际 `fansCnt`、`followCnt`、`likeCnt`、`userName` 和 `userKwaiId` 字段。
- 百家号：保留页面回退读取的全部指标，包括关注、点赞、评论、分享、收藏及 0 值。
- 失败提示：SQLite 损坏和身份资料未就绪给出明确提示，不再统一要求盲目重试。

## 验证与交付

- 8 个相关测试文件共 333 项测试通过；类型检查和生产构建通过。
- 第一轮安装版实测：抖音 3、小红书 3、视频号 3 均成功保存；百家号 2 个取得部分页面指标；哔哩哔哩 1 个完整取得 27 个作品，另 1 个取得概览但作品接口未返回可用结果。
- 另一个哔哩哔哩账号仍未登录，未绕过登录要求。
- 最终安装版复测：13 个账号最新采集均已写入数据，其中百家号 2 个及哔哩哔哩 1 个为部分采集。视频号停留超过身份观察有效期后仍可重新采集；其中一次页面加载出现 `ERR_TUNNEL_CONNECTION_FAILED`，单独重试后成功。
- 快手当前管理页显示登录提示，身份接口未提供有效身份，因此未强行采集或覆盖主页在线状态；仍需完成快手管理页登录。该项和未登录的哔哩哔哩账号未宣称实测成功。
- 数据库损坏的最初诱因尚未确认。恢复文件及真实采集后的完整性已检查，但不能据此保证以后不会损坏。
- 最终版本覆盖 `D:\SJdownloads\创作平台`，时间和校验值见 `output/collection-repair-installed-update.json`。最终逐账号结果见 `output/collection-repair-live-results.json`。
- 最终关闭程序后的完整校验通过：2,581 条历史指标逐条保持一致，新增 318 条，15 个国内账号、2 个国外账号及 46 个作品全部保留；外键错误为 0。见 `output/collection-repair-final-integrity.json`。
- 21:21 已用正常参数重启安装版，临时调试参数和端口均关闭，安装文件哈希与最终包一致。见 `output/collection-repair-final-restart.json`。
