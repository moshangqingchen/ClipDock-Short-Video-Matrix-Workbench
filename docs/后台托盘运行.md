# 后台托盘运行

2026-09-11 已更新安装目录 `D:\SJdownloads\创作平台`。

- Windows 点击关闭或 Alt+F4：隐藏工作台，保留同一进程、账号会话和后台任务；采集继续遵守原有开关、间隔和网络条件。
- 点击托盘图标、托盘“打开工作台”或再次启动软件：恢复同一窗口。
- 托盘“退出软件”：走完整退出流程，停止任务、关闭账号浏览器、保存会话并关闭数据库。
- Chrome 主页面及官方弹窗随工作台隐藏、恢复。托盘创建失败时保留正常关闭退出，避免留下无法恢复的隐藏程序。
- 未启用开机自启动。

验证：类型检查、生产构建、32 项相关测试、安装包构建与完整安装包冒烟测试通过。真实 Electron 运行时验证后台计时器、关闭恢复、重复启动恢复，以及菜单退出完成。Chrome 临时账号实机验证三种窗口尺寸、后台页面执行、已有及新建弹窗隐藏恢复、多账号和进程清理。

首次 Chrome 测试在窗口相关检查全部通过后遇到 Windows 进程查询瞬时失败；再次完整运行通过。未使用模拟结果代替真实 Chrome 结果。

安装版实测主进程 103824：关闭后无可见主窗口但进程响应正常；重新启动恢复原窗口 5967414，未创建第二个工作台。

记录：`docs/.compare/tray-tests.log`、`tray-runtime-smoke.log`、`tray-native-smoke-retry.log`、`tray-packaged-smoke.log`、`tray-package.log`、`tray-installed.json`。

升级前已正常关闭旧版并备份可执行文件、程序包、Chrome helper 和数据库至 `D:\SJdownloads\创作平台-backup-20260911-tray`；保留原账号登录目录与素材。替换文件均通过 SHA-256 校验。
