# 账号列表重复消失排查与加固

- 原安装版普通启动后两次显示 0 个账号，点击刷新仍为空；之前使用远程调试启动的验证不足以证明日常启动正常。
- 数据库副本完整性正常，账号表普通读取、排序读取均为 15 条，全部历史指标和作品仍在。
- 临时诊断版记录后台查询 15 条、返回界面 15 条；重新打包后普通启动窗口显示 15 个账号、14 个在线。临时诊断未捕获到最早空列表时的返回值，因此尚不能断言最初诱因。
- 正式代码增加账号读取数量校验；读取失败自动重试一次；持续失败时保留已有账号并显示明确错误。启动及重新加载事件处理读取拒绝，避免无提示失败。临时文件日志和额外诊断查询已移除。
- 6 个测试文件共 28 项通过，类型检查和生产构建通过。程序已覆盖安装，76 个程序文件校验通过，见 `output/account-list-repair-install.json`。
- 修复中未修改账号浏览器分区或登录 Cookie，未回滚数据库。最终完整性检查通过，原 2,581 条指标逐条一致，现为 3,215 条；15 个国内账号、2 个国外账号和 46 个作品全部保留。
- 正式安装版在不带远程调试参数、不重定向日志的情况下，通过桌面启动工具完整退出并启动两次，实际窗口均显示 15 个账号、14 个在线，头像、粉丝和总览指标可见。最终保持第二次普通启动的窗口打开；调试端口关闭，见 `output/account-list-repair-final-runtime.json`。
