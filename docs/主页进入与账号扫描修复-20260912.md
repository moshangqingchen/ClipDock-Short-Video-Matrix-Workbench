# 主页进入与账号扫描修复（2026-09-12）

## 使用变化

- 每次打开、切换或再次点击国内账号，抖音、快手、小红书、哔哩哔哩进入平台公共主页；没有公共网页主页的百家号、视频号进入管理页。
- 点击顶部“管理”才切换到创作者后台。弹窗关闭、网络恢复、账号资料更新保留正在浏览的页面。
- 登录检查成功不再定时强制跳转管理页。前台页面加载前就标记为可见，避免后台采集抢占导航。

## 扫描修复

- 创作者身份接口与资料采集限制在对应页面上下文；普通主页、登录页、验证页或加载中页面不能作为创作者资料来源。B站身份检查保留其受支持的公共主页上下文。
- 可见公共主页不自动跳后台，也不从推荐内容提取当前账号的昵称、UID、粉丝信息；隐藏页面允许切到采集工作页。登录/验证流程不被采集打断。
- 资料请求完成时检查页面实例、导航、URL、身份、账号状态和请求新旧顺序。切页、身份出现/清空/改变、旧请求返回时保留已保存资料。
- 缺失、空白或无效的扫描字段不覆盖存量昵称、账号标识；扫描期间用户修改的昵称也会保留。严格网络模式原有头像缓存/引用规则保持不变。
- 已在线账号的一次未登录响应先保留上次结论，3秒后使用新的检查结果复核；确认再次失败才标为掉线，成功则清除负面记录。明确的登录页证据仍可确认掉线。

## 排查证据与边界

只读审计发现当日小红书存在三次HTTP 401后标为离线、随后恢复在线的记录，支持扫描误报的排查方向；这不能证明平台服务器是否真的终止过会话。实际数据库中的13个国内账号仍保留平台标识。本次未重置、清除或迁移真实账号分区，也未调用真实账号接口做复测。

## 验证与交付

- 全量测试：160个文件、4287项通过；随后补充两项身份出现/清空回归，账号服务、采集和审核目录共106项定向测试通过。
- TypeScript检查、定向ESLint、生产构建通过。
- 新程序通过独立临时数据目录的Electron打包启动及界面检查；该检查不使用真实账号。
- 网络审核测试区分历史审核声明与当前源码；生产审核哈希、范围和旧证据均未更改，当前不匹配实现保持未审核。
- 新程序：`output/homepage-scan-fix-20260912/win-unpacked/短视频矩阵工作台.exe`。整个`win-unpacked`目录需一起保留，不能单独移动exe。
- 2026-09-12 16:58 已将修复包更新至原安装目录`D:\SJdownloads\创作平台`并重新启动，原快捷方式继续有效。不要使用项目的`启动工作台.cmd`代替，它使用另一套工作区数据目录。

验证日志：`output/homepage-scan-tests.log`、`output/homepage-scan-smoke.log`、`output/homepage-scan-package.log`。

## 原位置交付复核

用户反馈仍进管理页后，确认原安装app.asar仍是旧包（没有第三个主页入口参数），而前次新包仅在output目录。此次通过Windows安装更新的正常退出请求关闭旧程序，完整备份并逐文件校验后覆盖原安装文件。

- 备份：`D:\SJdownloads\创作平台备份\20260912-165641-164a5d`，21212个文件、约4.08GB，SHA256校验通过。
- 更新核对76个包文件，替换exe、app.asar和2个原生宿主文件；没有清除或迁移账号数据。
- 数据完整性通过：13个国内账号、2个国外账号、46条作品、1535条数据快照全部保留，账号浏览器目录全部保留。
- 新app.asar SHA256：`28c2ad78df319b23f06eb155a1a039763bfd20421d0610cef270fec34da5f885`。
- 原位置运行的界面实测点击小红书账号，页面地址为`https://www.xiaohongshu.com/explore`。当时用户处于代理模式，既有规则暂停国内网页，因此验证了主页入口选择，未宣称平台内容已加载；没有修改代理设置。
- 安装报告：`output/homepage-installed-update.json`；备份日志：`output/homepage-installed-backup.log`。
